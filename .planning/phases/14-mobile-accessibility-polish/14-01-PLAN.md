---
phase: 14-mobile-accessibility-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/two-panel-layout.tsx
  - apps/web/components/skills-table.tsx
autonomous: true

must_haves:
  truths:
    - "On mobile viewport (<640px), panels stack vertically"
    - "Skills table appears above leaderboard when stacked"
    - "Table is horizontally scrollable on narrow viewports"
  artifacts:
    - path: "apps/web/components/two-panel-layout.tsx"
      provides: "Responsive grid layout with sm: breakpoint"
      contains: "sm:grid-cols-3"
    - path: "apps/web/components/skills-table.tsx"
      provides: "Overflow wrapper for horizontal scroll"
      contains: "overflow-x-auto"
  key_links:
    - from: "two-panel-layout.tsx"
      to: "skills-table.tsx"
      via: "left panel slot"
      pattern: "sm:col-span-2"
---

<objective>
Update responsive breakpoints so the two-panel layout stacks on mobile (below 640px) with skills table above leaderboard, and add horizontal scroll to the table for narrow viewports.

Purpose: Satisfies LAYT-02 requirement - mobile users see a usable single-column layout
Output: Responsive layout that stacks at sm breakpoint with scrollable table
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mobile-accessibility-polish/14-CONTEXT.md
@.planning/phases/14-mobile-accessibility-polish/14-RESEARCH.md
@apps/web/components/two-panel-layout.tsx
@apps/web/components/skills-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TwoPanelLayout responsive breakpoint</name>
  <files>apps/web/components/two-panel-layout.tsx</files>
  <action>
Change the grid breakpoint from `lg:` (1024px) to `sm:` (640px) so panels stack on phones but show side-by-side on tablets and up.

Update:
- `grid-cols-1 gap-6 lg:grid-cols-3` -> `grid-cols-1 gap-6 sm:grid-cols-3`
- `lg:col-span-2` -> `sm:col-span-2`
- `lg:col-span-1` -> `sm:col-span-1`

This ensures only phones (<640px) get the single-column stacked layout. The left panel (skills table) will naturally render first in DOM order, appearing above the right panel (leaderboard) when stacked.
  </action>
  <verify>
Inspect TwoPanelLayout with browser dev tools at 639px width - should show single column.
At 640px+ width - should show two-panel layout (2/3 + 1/3).
Run `grep -n "sm:grid-cols-3\|sm:col-span" apps/web/components/two-panel-layout.tsx` - should find 3 matches.
  </verify>
  <done>
TwoPanelLayout stacks panels below 640px viewport width, skills table above leaderboard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add horizontal scroll wrapper to SkillsTable</name>
  <files>apps/web/components/skills-table.tsx</files>
  <action>
Wrap the table in an `overflow-x-auto` container to enable horizontal scrolling when table is wider than viewport on mobile.

Update the outer container:
- Current: `<div className="overflow-hidden rounded-lg border border-gray-200">`
- New: `<div className="overflow-x-auto rounded-lg border border-gray-200">`

The `overflow-x-auto` allows horizontal scrolling while `overflow-hidden` was preventing it. Keep `rounded-lg` and `border` for visual styling.

Also ensure the table has `min-w-full` (already present) to maintain full width and trigger scroll when needed.
  </action>
  <verify>
Resize browser to 400px width and verify table scrolls horizontally.
Run `grep -n "overflow-x-auto" apps/web/components/skills-table.tsx` - should find 1 match.
TypeScript compiles: `cd /home/claude/projects/relay && pnpm exec tsc --noEmit`
  </verify>
  <done>
Skills table scrolls horizontally on narrow viewports instead of breaking layout.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm exec tsc --noEmit` passes
2. At 639px viewport: single column layout, table scrolls horizontally
3. At 640px+ viewport: two-panel layout (skills 2/3, leaderboard 1/3)
4. Skills table appears above leaderboard when stacked
</verification>

<success_criteria>
- TwoPanelLayout uses `sm:` breakpoint (640px) for grid columns
- Table wrapper has `overflow-x-auto` class
- Mobile layout shows skills above leaderboard
- Table content is accessible via horizontal scroll on narrow screens
</success_criteria>

<output>
After completion, create `.planning/phases/14-mobile-accessibility-polish/14-01-SUMMARY.md`
</output>
