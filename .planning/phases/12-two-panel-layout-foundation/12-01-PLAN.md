---
phase: 12-two-panel-layout-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/two-panel-layout.tsx
  - apps/web/components/skills-table.tsx
autonomous: true

must_haves:
  truths:
    - "TwoPanelLayout renders children in 2/3 + 1/3 grid"
    - "SkillsTable displays 6 columns with proper headers"
    - "Table rows render skill data with all required fields"
  artifacts:
    - path: "apps/web/components/two-panel-layout.tsx"
      provides: "Two-panel responsive grid layout"
      exports: ["TwoPanelLayout"]
    - path: "apps/web/components/skills-table.tsx"
      provides: "Skills table with 6-column structure"
      exports: ["SkillsTable", "SkillsTableProps"]
  key_links:
    - from: "apps/web/components/two-panel-layout.tsx"
      to: "children props"
      via: "React children slot pattern"
      pattern: "children.*ReactNode"
    - from: "apps/web/components/skills-table.tsx"
      to: "shadcn/ui Table"
      via: "component imports"
      pattern: "from.*@/components/ui/table"
---

<objective>
Create the foundational UI components for the two-panel layout redesign: a responsive grid wrapper and a 6-column skills table.

Purpose: Establish the visual structure that will replace the current card-based browse page. These components are pure presentation without data fetching, making them reusable and testable.

Output: Two new components ready for integration in Plan 02.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@apps/web/components/skill-card.tsx
@apps/web/components/leaderboard-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TwoPanelLayout component</name>
  <files>apps/web/components/two-panel-layout.tsx</files>
  <action>
Create a Server Component that renders a responsive two-panel grid layout.

Structure:
- CSS Grid with `grid-cols-1 lg:grid-cols-3` (stacks on mobile, 3-col on desktop)
- Left panel: `lg:col-span-2` (2/3 width on desktop)
- Right panel: `lg:col-span-1` (1/3 width on desktop)
- Gap of 6 between panels (`gap-6`)
- Max width container with horizontal padding (match existing page.tsx: `max-w-6xl mx-auto px-4`)

Props interface:
```typescript
interface TwoPanelLayoutProps {
  left: React.ReactNode;
  right: React.ReactNode;
  className?: string;
}
```

Do NOT use "use client" - this is a Server Component (pure layout, no interactivity).
  </action>
  <verify>
1. File exists at apps/web/components/two-panel-layout.tsx
2. Exports TwoPanelLayout component
3. No "use client" directive present
4. Uses grid layout with lg:col-span-2 and lg:col-span-1
  </verify>
  <done>TwoPanelLayout component renders left/right children in 2/3 + 1/3 responsive grid</done>
</task>

<task type="auto">
  <name>Task 2: Create SkillsTable component</name>
  <files>apps/web/components/skills-table.tsx</files>
  <action>
Create a Server Component that renders skills in a table format with 6 columns.

Columns (in order):
1. Skill Name - left aligned, text-sm font-medium
2. Days Saved - right aligned, calculated as (totalUses * hoursSaved) / 8
3. Installs - right aligned, totalUses field
4. Date Added - right aligned, format as "MMM D, YYYY" (e.g., "Jan 15, 2026")
5. Author - left aligned, author.name with fallback to "Anonymous"
6. Sparkline - center aligned, use existing Sparkline component

Use shadcn/ui Table components (already installed):
- Import from "@/components/ui/table"
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow

Props interface - extend the existing SkillCardData pattern:
```typescript
interface SkillTableRow {
  id: string;
  name: string;
  slug: string;
  totalUses: number;
  hoursSaved: number | null;
  createdAt: Date;  // New field for date added
  author: {
    id: string;
    name: string | null;
  } | null;
}

interface SkillsTableProps {
  skills: SkillTableRow[];
  usageTrends: Map<string, number[]>;
}
```

Styling:
- Use existing leaderboard-table.tsx as reference for table styling patterns
- Alternating row colors: odd rows white, even rows gray-50
- Hover state: bg-blue-50 transition
- Skill name should be a Link to `/skills/${slug}`

Do NOT add sorting controls yet - that's Phase 13. This is the static table structure.
Do NOT use "use client" - Server Component with no interactivity.
  </action>
  <verify>
1. File exists at apps/web/components/skills-table.tsx
2. Exports SkillsTable component and SkillsTableProps interface
3. Has 6 column headers: Skill Name, Days Saved, Installs, Date Added, Author, Sparkline
4. Uses shadcn/ui Table components
5. Renders Sparkline component in last column
6. No "use client" directive
7. TypeScript compiles: `npx tsc --noEmit -p apps/web/tsconfig.json`
  </verify>
  <done>SkillsTable component renders skills with all 6 columns using shadcn/ui Table</done>
</task>

</tasks>

<verification>
After both tasks:
1. Both files exist and export their components
2. TypeScript compilation succeeds: `npx tsc --noEmit -p apps/web/tsconfig.json`
3. No "use client" in either file (both are Server Components)
4. SkillsTable imports Sparkline from "./sparkline"
</verification>

<success_criteria>
- TwoPanelLayout renders responsive 2/3 + 1/3 grid layout
- SkillsTable displays 6 columns with headers and row rendering
- Both components are Server Components (no "use client")
- TypeScript compiles without errors
- Ready for integration in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/12-two-panel-layout-foundation/12-01-SUMMARY.md`
</output>
