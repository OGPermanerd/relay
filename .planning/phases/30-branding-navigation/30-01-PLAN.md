---
phase: 30-branding-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/animated-logo.tsx
  - apps/web/app/globals.css
autonomous: true

must_haves:
  truths:
    - "An animated SVG logo renders a baton-pass relay concept"
    - "The logo accepts a size prop for default and small variants"
    - "CSS keyframe animations run without any new npm dependencies"
  artifacts:
    - path: "apps/web/components/animated-logo.tsx"
      provides: "AnimatedLogo client component"
      exports: ["AnimatedLogo"]
    - path: "apps/web/app/globals.css"
      provides: "CSS keyframe animations for relay logo"
      contains: "@keyframes relay"
  key_links:
    - from: "apps/web/components/animated-logo.tsx"
      to: "apps/web/app/globals.css"
      via: "CSS class names"
      pattern: "animate-relay"
---

<objective>
Create an animated SVG relay logo component with CSS keyframe animations.

Purpose: BRAND-01 requires an animated baton-pass concept logo to replace the plain text "EverySkill" link.
Output: `AnimatedLogo` client component + CSS keyframes in globals.css.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimatedLogo component + CSS keyframes</name>
  <files>apps/web/components/animated-logo.tsx, apps/web/app/globals.css</files>
  <action>
1. Create `apps/web/components/animated-logo.tsx` as a `"use client"` component:
   - Export `AnimatedLogo` with props `{ size?: "default" | "small" }`.
   - `size="default"`: SVG 32x32, text "EverySkill" in `text-xl font-bold text-gray-900`.
   - `size="small"`: SVG 24x24, text "EverySkill" in `text-sm font-bold text-gray-900`.
   - SVG viewBox="0 0 32 32" containing a baton-pass relay concept:
     - Left circle (cx=10, cy=16, r=4, fill="#0ea5e9") with class `animate-relay-left`.
     - Right circle (cx=22, cy=16, r=4, fill="#6366f1") with class `animate-relay-right`.
     - Baton rect (x=12, y=14, width=8, height=4, rx=2, fill="#0ea5e9") with class `animate-relay-baton`.
   - Wrap SVG + text in `<div className="flex items-center gap-1.5">`.

2. Append CSS keyframes to `apps/web/app/globals.css` (after existing styles):
   ```css
   /* Relay logo animation */
   @keyframes relay-left {
     0%, 100% { transform: translateX(0); }
     50% { transform: translateX(4px); }
   }
   @keyframes relay-right {
     0%, 100% { transform: translateX(0); }
     50% { transform: translateX(-4px); }
   }
   @keyframes relay-baton {
     0%, 100% { opacity: 1; }
     50% { opacity: 0.6; transform: scaleX(1.2); }
   }
   .animate-relay-left { animation: relay-left 3s ease-in-out infinite; }
   .animate-relay-right { animation: relay-right 3s ease-in-out infinite; }
   .animate-relay-baton { animation: relay-baton 3s ease-in-out infinite; }
   ```

Do NOT add framer-motion or any animation library. CSS keyframes only.
  </action>
  <verify>
Run `cd /home/dev/projects/relay && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20` to confirm no type errors in the new component.
Visually confirm the CSS classes are appended to globals.css.
  </verify>
  <done>
AnimatedLogo component exists at `apps/web/components/animated-logo.tsx`, renders an animated SVG relay logo with two size variants. CSS keyframes exist in globals.css. No new npm dependencies added.
  </done>
</task>

</tasks>

<verification>
- `apps/web/components/animated-logo.tsx` exports `AnimatedLogo`.
- `apps/web/app/globals.css` contains `@keyframes relay-left`, `relay-right`, `relay-baton`.
- TypeScript compiles without errors.
</verification>

<success_criteria>
AnimatedLogo component renders an animated baton-pass SVG logo with "EverySkill" text, supporting "default" and "small" size variants, powered entirely by CSS keyframes.
</success_criteria>

<output>
After completion, create `.planning/phases/30-branding-navigation/30-01-SUMMARY.md`
</output>
