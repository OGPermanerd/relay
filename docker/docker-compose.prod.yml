services:
  caddy:
    build:
      context: .
      dockerfile: Dockerfile.caddy
    container_name: everyskill-caddy
    ports:
      - "178.156.181.178:80:80"
      - "178.156.181.178:443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      HETZNER_DNS_API_TOKEN: ${HETZNER_DNS_API_TOKEN}
      STAGING_AUTH_USER: ${STAGING_AUTH_USER}
      STAGING_AUTH_HASH: ${STAGING_AUTH_HASH}
    depends_on:
      web-prod:
        condition: service_healthy
      web-staging:
        condition: service_healthy
    networks:
      - public
      - internal
    restart: unless-stopped

  web-prod:
    image: ghcr.io/ogpermanerd/relay:production
    container_name: everyskill-web-prod
    environment:
      DATABASE_URL: postgresql://everyskill:${DB_PASSWORD}@postgres:5432/everyskill
      NEXTAUTH_URL: https://everyskill.ai
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_GOOGLE_ID: ${AUTH_GOOGLE_ID}
      AUTH_GOOGLE_SECRET: ${AUTH_GOOGLE_SECRET}
      NEXT_PUBLIC_ROOT_DOMAIN: everyskill.ai
      NEXT_PUBLIC_APP_URL: https://everyskill.ai
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      VOYAGE_API_KEY: ${VOYAGE_API_KEY}
      R2_ENDPOINT: ${R2_ENDPOINT}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
      R2_BUCKET_NAME: ${R2_BUCKET_NAME}
      NODE_ENV: production
      PORT: 2000
    expose:
      - "2000"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:2000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - internal
    restart: unless-stopped

  web-staging:
    image: ghcr.io/ogpermanerd/relay:staging
    container_name: everyskill-web-staging
    environment:
      DATABASE_URL: postgresql://everyskill:${DB_PASSWORD}@postgres:5432/everyskill_staging
      NEXTAUTH_URL: https://staging.everyskill.ai
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_GOOGLE_ID: ${AUTH_GOOGLE_ID}
      AUTH_GOOGLE_SECRET: ${AUTH_GOOGLE_SECRET}
      NEXT_PUBLIC_ROOT_DOMAIN: everyskill.ai
      NEXT_PUBLIC_APP_URL: https://staging.everyskill.ai
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      VOYAGE_API_KEY: ${VOYAGE_API_KEY}
      R2_ENDPOINT: ${R2_ENDPOINT}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
      R2_BUCKET_NAME: ${R2_BUCKET_NAME}
      NODE_ENV: production
      PORT: 2001
    expose:
      - "2001"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:2001/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - internal
    restart: unless-stopped

  postgres:
    image: pgvector/pgvector:pg17
    container_name: everyskill-postgres
    environment:
      POSTGRES_USER: everyskill
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: everyskill
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-databases.sh:/docker-entrypoint-initdb.d/init-databases.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U everyskill"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - internal
    restart: unless-stopped
    # NO ports: section -- database is internal only (SOC2-07)

volumes:
  postgres_data:
  caddy_data:
  caddy_config:

networks:
  internal:
    internal: true
  public:
